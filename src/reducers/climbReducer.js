const initialState = {
    routes: [],
    error: "", 
    isFetching: false
}

export const climbReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}